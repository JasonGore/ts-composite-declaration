{
  "name": "ts-composite-declaration",
  "version": "1.0.0",
  "description": "composite and emitDeclarationOnly repro",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
